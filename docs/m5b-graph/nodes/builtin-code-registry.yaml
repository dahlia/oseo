version: 1
id: builtin-code-registry
title: "Built-in code ID and dispatch registry"
delivers: "Replaces the hand-numbered SIZE_MAX code-ID block and the single
  else-if dispatch chain with per-component ID ranges and table-driven
  dispatch so later families stop editing one shared block."
dependencies: []
landed: false
status: ready

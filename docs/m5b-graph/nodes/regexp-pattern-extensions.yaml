version: 1
id: regexp-pattern-extensions
title: "Unicode sets, modifiers, and assertions"
delivers: "Adds class set notation, inline modifiers, named groups,
  lookbehind, match indices, and dotAll matching."
inventory:
  included: 255
  roots:
    - test/built-ins/RegExp/dotall/
    - test/built-ins/RegExp/lookBehind/
    - test/built-ins/RegExp/match-indices/
    - test/built-ins/RegExp/named-groups/
    - test/built-ins/RegExp/regexp-modifiers/
    - test/built-ins/RegExp/unicodeSets/
dependencies:
  - regexp-unicode-property-escapes
landed: false
status: parked
reason: "Mandatory root review round five raised a q-string singleton
  semantic finding. A Claude Opus correction dispatch derived the branch's
  behavior from ECMA-262, added discriminating evidence in commit cabe2063,
  and passed Codex cross-review and all three gates, but that new delta still
  requires another root codex review and the coordinator review limit is five.
  Resume the preserved m5b-regexp-extensions branch only with authority to
  restart or extend that root-review budget."

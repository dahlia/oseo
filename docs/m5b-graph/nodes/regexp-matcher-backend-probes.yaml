version: 1
id: regexp-matcher-backend-probes
title: "Matcher backend probes and selection"
delivers: "Runs the matcher-strategy, external-component, Unicode-table,
  resource, and code-size probes and reports the measured candidates without
  selecting a backend."
dependencies:
  - regexp-literal-aot
landed: true
status: ready

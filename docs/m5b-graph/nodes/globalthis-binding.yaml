version: 1
id: globalthis-binding
title: "globalThis binding"
delivers: "Exposes the global object as a value, retains property-backed cells
  for known globals, and falls back to realm global-object lookup for an
  unresolved global reference."
inventory:
  included: 29
  roots:
    - test/built-ins/global/
dependencies:
  - global-object-record
  - reviewed-evidence-direction-policy
  - runtime-error-observation
landed: false
status: parked
reason: "The port exposes five out-of-root global-code rows where assert.throws
  catches an absent $262 binding and 453 RegExp rows whose reviewed harness
  omits required helper definitions. A maintainer must decide whether parsed
  AST and harness metadata may preclassify those capabilities as unsupported
  before manifest regeneration."

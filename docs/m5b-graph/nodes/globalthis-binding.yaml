version: 1
id: globalthis-binding
title: "globalThis binding"
delivers: "Would expose the global object as a value, admitting dynamically
  created global bindings."
inventory:
  included: 29
  roots:
    - test/built-ins/global/
dependencies:
  - global-object-record
landed: false
status: parked
reason: "Admitting it needs an accepted architecture decision reconciling a
  mutable global object with closed-world name resolution, which
  docs/language-profile-m5.md assigns to this stream. That decision is costly
  to reverse and is maintainer judgment, not implementation work."

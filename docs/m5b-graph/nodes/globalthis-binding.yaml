version: 1
id: globalthis-binding
title: "globalThis binding"
delivers: "Exposes the global object as a value, retains property-backed cells
  for known globals, and falls back to realm global-object lookup for an
  unresolved global reference."
inventory:
  included: 29
  roots:
    - test/built-ins/global/
dependencies:
  - global-object-record
landed: false
status: parked
reason: "The runtime implementation is preserved in m5b-globalthis-binding,
  but five reviewed global-code rows need a sound unavailable-$262 capability
  boundary and four out-of-root negative rows require a policy decision before
  they may move from unsupported-profile-feature to expected-negative."

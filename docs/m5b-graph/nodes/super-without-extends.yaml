version: 1
id: super-without-extends
title: "super in a null-extends home object"
delivers: "Admits a super property reference in a class body without extends
  and in an object literal method, whose lookup now reaches
  %Object.prototype%."
dependencies:
  - object-prototype
landed: false
status: parked
reason: "Three Codex worker starts timed out at Orca agent_readiness before
  implementation, review, or gates could resume. Preserve the dirty
  m5b-super-without-extends worktree; the failure is in worker startup rather
  than the node implementation."

@oseo/parser-babel
==================

This package contains the bootstrap Babel adapter. It converts parser failures
to Oseo diagnostics and does not expose Babel syntax nodes through its public
API.
